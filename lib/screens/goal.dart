import 'package:flutter/material.dart';

class GoalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Goal'),
      ),
      body: Center(
        child: Text('This is the Goal Page'),
      ),
    );
  }
}
